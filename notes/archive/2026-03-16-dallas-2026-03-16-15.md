# Dallas Learnings — 2026-03-16 (PR-4970128 Review Fix — Nth Dispatch)

## Task
Fix review issues on PR-4970128: feat(PL-W015): add CoT streaming and ask-user-question protocol types

## Outcome
**Early bail-out** — no code changes needed. PR already fully approved with all review feedback addressed.

## Findings

### 1. Engine dispatch continues to re-dispatch resolved PRs
PR-4970128 has received 17+ dispatch cycles despite Yemi Shin vote: 10 (APPROVE), 3 commits with all fixes applied, and 16+ closed APPROVE threads. The dispatched "review findings" were implementation notes ("Build: PASS | Tests: 110 passed | Downstream core: PASS"), not actionable code feedback. (source: PR-4970128, ADO REST API thread listing)

### 2. Early bail-out pattern saves significant compute
Checking PR thread history via ADO REST API (`GET /pullRequests/{prId}/threads?api-version=7.1`) before creating worktrees or running builds saves 5-10 minutes per dispatch. Total time for this bail-out: ~15 seconds. (source: ADO REST API `https://dev.azure.com/office/ISS/_apis/git/repositories/61458d25-9f75-41c3-be29-e63727145257/pullRequests/4970128/threads?api-version=7.1`)

### 3. MCP ADO tools unavailable — REST API fallback works
`mcp__azure-ado__repo_create_pull_request_thread` was not available via ToolSearch. Used curl + Bearer token from `az account get-access-token --resource 499b84ac-1321-427f-aa17-267ca6975798` as reliable fallback. (source: ToolSearch returned "No matching deferred tools found")

### 4. Windows bash path format
Must use `/c/Users/yemishin/.squad` (POSIX) not `C:\Users\yemishin\.squad` (Windows) in bash `cd` commands. (source: bash error "No such file or directory" with Windows-style path)

## Patterns Reinforced
- **Early bail-out for PR review fixes**: Check existing reviewer votes and thread history before any work
- **ADO REST API via curl**: Write JSON body to `$TEMP/file.json`, use `-d @"$TEMP/file.json"` to avoid shell escaping issues
- **Closed-status threads (status: 4)**: Use for "no action needed" confirmations
