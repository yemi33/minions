# Ripley Learnings — 2026-03-16 (PR-4970128 Review, 4th pass)

## Task
Review PR-4970128: feat(PL-W015): add CoT streaming and ask-user-question protocol types

## Verdict
**APPROVE** (vote: 10) — Thread ID: 62177684

## Patterns Confirmed

### MessageType enum conventions
- New values appended at end of enum, snake_case string values, section comments for grouping
  (source: `modules/message-protocol/src/types/message-type.ts:165-171`)

### Three-tier CoT type system complete
1. `WorkspaceChainOfThoughtPayload` — batch, string content (pre-existing)
2. `PptAgentCotPayload` — typed batch with contentType/turnNumber/toolName (pre-existing, source: `modules/message-protocol/src/types/agents/ppt-agent/messages.ts`)
3. `ChainOfThoughtUpdatePayload` — incremental streaming with discriminated event union (this PR, source: `modules/message-protocol/src/types/chain-of-thought-stream.ts:125-132`)

### CoTStreamEventKind alignment with PptAgentCotContentType
- Three shared values: 'thinking', 'text', 'tool_use'
- Three additional streaming kinds: 'step_started', 'step_completed', 'ask_user_question'
- JSDoc documents intentional alignment (source: `modules/message-protocol/src/types/chain-of-thought-stream.ts:14-16`)

### Ask-user protocol direction modeling
- `AskUserQuestionMessage = Message<AskUserQuestionPayload>` — server→client (plain Message)
- `UserAnswerMessage = ResponseMessage<UserAnswerPayload>` — client→server (has requestId)
  (source: `modules/message-protocol/src/types/ask-user-question.ts:59-62`)

### Compile-time shape tests pattern
- Tests create objects with explicit type annotations; field renames cause compile failures
- Combined with runtime expect() assertions for complete coverage
  (source: `modules/message-protocol/tests/message-type.test.ts:428-591`)

## ADO API Pattern (reinforced)
- Node.js script with https module is most reliable for Windows; avoids bash escaping issues with backticks in review content
- Write review body to temp .md file, read in Node.js script, avoids all shell escaping problems
- VSID: GET `dev.azure.com/office/_apis/connectionData?api-version=6.0-preview` → `authenticatedUser.id`
- Vote: PUT `.../reviewers/{vsid}?api-version=7.1` with `{vote: 10}`

## Gotchas
- **Node.js `-e` flag with backticks in content causes bash expansion**: When review content contains backticks, using `node -e` causes bash to interpret them as command substitution. Solution: write a .js file instead of inline `-e` eval.
  (source: this session — repeated failures with `node -e` approach)

## Future Action Items
- Extract shared CoT type base between `CoTStreamEventKind` and `PptAgentCotContentType`
- Adapt `ChainOfThoughtContentNotifier` to emit structured `CoTStreamEvent` objects for WebSocket streaming handler
  (source: `modules/chain-of-thought/src/content-handler.ts`)
