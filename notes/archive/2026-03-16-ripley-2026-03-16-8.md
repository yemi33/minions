# Ripley Learnings — 2026-03-16 (PR-4970916 Re-review)

## Task
Re-review of PR-4970916: feat(PL-W009): add cowork host integration demo and test fixtures

## Findings

### PR Status
- **Verdict**: APPROVE (vote: 10)
- **Thread ID**: 62173655
- All 5 previously flagged issues were fixed in commits `9e530474e` and `8304aad78`
- (source: PR-4970916, branch `feat/PL-W009-host-integration-demo`)

### Patterns Confirmed

1. **`.devtools/` packages use `@officeagent-tools/` scope** — cowork-demo follows this correctly (source: `.devtools/cowork-demo/package.json`)

2. **`console.*` allowed in `.devtools/`** — production logging rules don't apply to dev tooling packages (source: CLAUDE.md logging rules section)

3. **Mock AugLoop server on port 11040** — matches the dev endpoint convention from `.devtools/test-client/src/augloop-client.ts` (source: `.devtools/cowork-demo/src/mock-transport/mock-augloop-server.ts`)

4. **Canned fixture pattern with `readonly` fields and `delay_ms` timing** — established pattern for event sequence testing (source: `.devtools/cowork-demo/src/fixtures/cot-events.ts`, `progression-steps.ts`, `ask-user-events.ts`)

5. **Host environment presets** — 4 presets (standalone, iframe_word, iframe_teams, iframe_generic) model real deployment scenarios (source: `.devtools/cowork-demo/src/mock-transport/host-environment.ts`)

### Fix Patterns Worth Noting

6. **`wrappedResolve` pattern for timer leak prevention** — When a Promise has both a user-triggered resolve and a timeout fallback, wrap the resolve to clear the timeout timer on user action. Prevents dangling timers. (source: `mock-augloop-server.ts:runFullInteractiveScenario`)

7. **Runtime type guard for string union validation** — Instead of `as ScenarioName` cast, use `isValidScenario()` type guard with `includes()` check against a readonly array. Print valid options on failure. (source: `demo-server.ts:isValidScenario`)

8. **Lazy default value generation** — Don't call `uuidv4()` at module load time in `DEFAULT_OPTIONS`. Instead, generate per-call with spread: `userId: mock-user-${uuidv4().slice(0, 8)}, ...options`. Preserves deterministic test behavior. (source: `mock-token-provider.ts:createMockTokenProvider`)

### ADO API Gotchas

9. **Windows node.js tmpdir differs from bash `/tmp`** — `os.tmpdir()` returns `C:\Users\<user>\AppData\Local\Temp` but bash `/tmp` maps differently. Always use `os.tmpdir()` in node code, never hardcode `/tmp/`. (source: encountered during review posting)

10. **`office.visualstudio.com` returns 302 redirect without proper auth** — Must use `dev.azure.com` hostname AND export token to environment before spawning node process. The `ADO_TOKEN` env var must be explicitly exported. (source: encountered during review posting)

11. **ADO thread creation returns thread ID in `id` field** — POST to `pullRequests/{prId}/threads` returns `{ id: number }` on success (status 200). (source: thread 62173655 on PR-4970916)

12. **VSID retrieval for vote**: GET `dev.azure.com/office/_apis/connectionData?api-version=6.0-preview` → `authenticatedUser.id` gives VSID `1c41d604-e345-64a9-a731-c823f28f9ca8`. (source: connectionData API response)
