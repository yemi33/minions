# Ripley Learnings — 2026-03-17

## Task: Review PR-4976445 (Duplicate Dispatch)

### Context
- **PR**: feat(PL-W001): consolidate cowork types for E2E branch integration
- **Branch**: `feat/PL-W001-e2e-consolidation-fix`
- **Latest commit**: `cdf36677dab0` (unchanged from prior review)

### Early Bail-Out Applied

This is the Nth+11 dispatch for PR-4976445 with zero code changes since my original review. Applied the early bail-out pattern:

1. Fetched branch — latest commit still `cdf36677dab0` (source: `git log --oneline origin/feat/PL-W001-e2e-consolidation-fix -5`)
2. Checked ADO threads API — 37 threads, 14 with APPROVE content (source: `dev.azure.com/office/OC/_apis/git/repositories/74031860-e0cd-45a1-913f-10bbf3f82555/pullRequests/4976445/threads`)
3. Thread count exceeds 30-thread threshold — **skipped thread posting** to avoid worsening ADO timeouts
4. Attempted vote resubmission (vote: 5, approve-with-suggestions)

### Patterns & Conventions

- **Thread count threshold for bail-out thread posting**: When thread count exceeds 30, skip posting new closed-status threads entirely. PR-4976445 is at 37 threads, PR-4976897 was at 58. Additional threads worsen ADO rate-limiting on all PUT/POST operations. (source: ADO REST API response for PR-4976445/threads, team notes from 2026-03-17)

- **Windows `/dev/stdin` incompatibility with Node.js persists**: `readFileSync('/dev/stdin')` resolves to `C:\dev\stdin` (ENOENT). Must use `readFileSync(0, 'utf8')` (file descriptor 0) instead. (source: error output from `node -e` piped command in this session)

- **Early bail-out pattern timing**: Pre-flight check (git fetch + commit SHA comparison + ADO threads API) completed in ~15 seconds vs 5-10 minutes for full review cycle. (source: this session timing)

### Prior Review Summary (unchanged)

My original review of PR-4976445 found:
- **49 new files** across `apps/bebop/src/features/cowork/` (34) and `packages/cowork-component/` (15)
- **Verdict**: APPROVE with suggestions (vote 5)
- **Key findings**:
  - `as unknown as` type assertions in syncFromSnapshotAtom violate CLAUDE.md no-assertion rule (source: `apps/bebop/src/features/cowork/atoms/coworkAtoms.ts:125-130`)
  - `console.error` in CoworkErrorBoundary violates logging guidelines (source: `apps/bebop/src/features/cowork/components/CoworkErrorBoundary/CoworkErrorBoundary.tsx:37-39`)
  - `server/` directory misnamed for client-side AugLoop code (source: `apps/bebop/CONTRIBUTING.md:221`)
  - Test coverage gap: 1 test file for 49 new files; `featureGates.test.ts` won't run in Jest CJS due to `import.meta.env`
  - `packages/cowork-component/` has zero test files despite 15 new modules

### Bugs & Gotchas

- **Engine dispatch deduplication still unimplemented**: PR-4976445 has been dispatched 11+ times with identical commit `cdf36677dab0`. Three pre-flight checks needed: (1) commit SHA comparison, (2) existing reviewer vote check, (3) thread count threshold >50 to skip dispatch entirely. (source: PR-4976445 dispatch history across multiple sessions)

- **ADO thread accumulation causes cascading timeouts**: At 37 threads, POST operations still work but are slower. At 58+ threads (PR-4976897), both PUT and POST operations time out. The inflection point appears to be around 40-50 threads. (source: comparing response times across PR-4976445 at 37 threads vs PR-4976897 at 58 threads)
