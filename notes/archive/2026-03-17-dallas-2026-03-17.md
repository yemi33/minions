# Dallas Learnings — 2026-03-17 (PR-4976897 Duplicate Dispatch)

## Summary
Duplicate dispatch for PR-4976897 (`feat/PL-W012-augloop-integration`). Commit `a0ab1d949aad` unchanged from all prior reviews. Applied early bail-out pattern successfully in ~15 seconds.

## Findings

### Early bail-out pattern confirmed effective
- PR-4976897 now has 55 threads with 37 APPROVE verdicts after this dispatch
- Pre-flight check (git fetch + log for SHA, ADO threads API for existing approvals) completes in ~15s vs 5-10 min full review cycle
- (source: `git log --oneline origin/feat/PL-W012-augloop-integration` showing `a0ab1d949aad`, ADO threads API showing 54→55 threads)

### ADO REST API vote submission working reliably
- PUT with `{"vote":10}` to `dev.azure.com/office/OC/_apis/git/repositories/74031860-e0cd-45a1-913f-10bbf3f82555/pullRequests/4976897/reviewers/{vsid}?api-version=7.1` returned HTTP 200 within 5s
- (source: this session — curl returned 200, no timeout)

### GCM credential retrieval still reliable
- `printf "protocol=https\nhost=office.visualstudio.com\n" | git credential fill` worked first try
- (source: this session)

### Thread count escalation concern
- PR-4976897 now at 55 threads. Each bail-out adds +1 thread. If engine continues dispatching, thread count will keep growing.
- **Action needed**: Engine must implement pre-flight checks: (1) compare commit SHAs, (2) check existing APPROVE votes, (3) filter keywords like "No Action Required", "Duplicate Dispatch", "early bail-out" from review findings classification
- (source: ADO threads API count: 54 before this session, 55 after)

### MCP ADO tools still unavailable
- `mcp__azure-ado__*` tools not discoverable via ToolSearch. REST API via curl + GCM Bearer token is the only working path.
- (source: known from prior sessions)

### Windows temp file pattern for curl JSON
- Write to `$TEMP/filename.json`, reference via `@"$TEMP/filename.json"`
- (source: this session — both thread POST and vote PUT used this pattern successfully)
