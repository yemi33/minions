# Dallas Learnings — 2026-03-16 (PR-4970916 Duplicate Dispatch — Nth+1 bail-out)

## Summary
PR-4970916 dispatched again for "fix review" despite identical 4 commits and multiple prior APPROVE verdicts. The "review findings" were my own prior bail-out note being misclassified as actionable feedback by the engine.

## Findings

### Engine dispatch still lacks pre-flight checks
- PR-4970916 continues to be re-dispatched with zero new commits (`05cbc73`, `b9240c9`, `9e53047`, `8304aad`) (source: `git log --oneline main...origin/feat/PL-W009-host-integration-demo`)
- The "reviewer comment" triggering this dispatch was authored by Dallas (me) — the engine is feeding agent bail-out notes back as "review findings to address"
- This is the same issue documented in `knowledge/build-reports/2026-03-16-dallas-dallas-learnings-2026-03-16-pr-4970916-8th-dispatc.md`

### Early bail-out pattern continues to save time
- Checking commit SHAs via `git fetch + git log` (~15s) vs full worktree setup + build + test + lint (5-10 min)
- Posted closed-status thread (status: 4) confirming no action needed (source: ADO REST API POST to `/pullRequests/4970916/threads`)

## Conventions
- Always check commit SHAs first on re-dispatched PRs before any worktree creation
- Use `status: 4` for "no action needed" confirmation threads on ADO PRs

## Action Items for Engine
- Engine dispatch must filter out agent-authored comments from "review findings" — feeding Dallas's own bail-out notes back as issues creates infinite loops
- Pre-flight checks (commit SHA comparison, existing APPROVE votes) remain unimplemented despite 10+ redundant dispatches across PR-4970916 and PR-4970128
