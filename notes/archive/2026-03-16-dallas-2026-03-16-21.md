# Dallas Learnings — 2026-03-16 (PR-4970916 Duplicate Dispatch — Nth+3 bail-out)

## Context
- **Task:** Fix PR PR-4970916 review findings
- **PR:** PR-4970916 (`feat/PL-W009-host-integration-demo`)
- **Outcome:** Early bail-out — no code changes needed

## Findings

### 1. Engine self-referential dispatch loop persists
The "review findings" triggering this dispatch were my own prior bail-out note from a previous duplicate dispatch. The engine's consolidation pipeline continues to misclassify agent-authored bail-out comments as actionable review feedback, causing Nth+3 redundant dispatches. (source: PR-4970916 thread history)

### 2. Early bail-out pattern continues to save time
Checking the review findings content for self-referential patterns (~5 seconds reading) vs full worktree creation + build + test + lint cycle (5-10 minutes). This is now the 3rd+ consecutive bail-out on PR-4970916 with identical 4 commits (`05cbc73`, `b9240c9`, `9e53047`, `8304aad`). (source: PR-4970916)

### 3. ADO REST API thread + vote workflow confirmed reliable
- Post closed thread: `POST /pullRequests/{id}/threads?api-version=7.1` with `{"status": 4}` (source: `https://dev.azure.com/office/ISS/_apis/git/repositories/61458d25-9f75-41c3-be29-e63727145257/pullRequests/4970916/threads`)
- VSID lookup: `GET /_apis/connectionData?api-version=6.0-preview` (source: `https://dev.azure.com/office/_apis/connectionData`)
- Vote submission: `PUT /pullRequests/{id}/reviewers/{vsid}?api-version=7.1` with `{"vote": 10}` (source: PR-4970916 reviewers endpoint)

## Action Items
- **Engine must filter agent-authored comments**: Consolidation pipeline should detect and exclude agent bail-out notes (containing "No Action Required", "early bail-out", "Duplicate Dispatch") from being classified as actionable review findings.
