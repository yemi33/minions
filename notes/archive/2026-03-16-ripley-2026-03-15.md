# Ripley Learnings — 2026-03-15 (PR-4970128 Review)

## Task
Reviewed PR-4970128: `feat(PL-W015): add CoT streaming and ask-user-question protocol types`

## Findings

### PR-4970128 adds 3 new MessageType enum values and 2 type files
- `ChainOfThoughtUpdate = 'chain_of_thought_update'` — incremental CoT streaming (source: `modules/message-protocol/src/types/message-type.ts:167`)
- `AskUserQuestion = 'ask_user_question'` — agent-to-client question (source: `modules/message-protocol/src/types/message-type.ts:170`)
- `UserAnswer = 'user_answer'` — client-to-agent answer (source: `modules/message-protocol/src/types/message-type.ts:171`)
- New file: `modules/message-protocol/src/types/chain-of-thought-stream.ts` — 118 lines, discriminated union on `kind` field
- New file: `modules/message-protocol/src/types/ask-user-question.ts` — 62 lines, bidirectional Message/ResponseMessage pattern

### Existing CoT types in codebase
- `ChainOfThoughtPayload` (simple `content: string`) in `modules/message-protocol/src/types/core.ts:266-269`
- `WorkspaceChainOfThoughtPayload` (batch/final-state) in `modules/message-protocol/src/types/agents/workspace-agent/types.ts:303`
- `PptAgentCotPayload` (typed with `contentType`, `turnNumber`, `toolName`) in `modules/message-protocol/src/types/agents/ppt-agent/messages.ts:286-295`
- `ChainOfThoughtContentNotifier` interface in `modules/chain-of-thought/src/content-handler.ts:4-5` — has `sendChainOfThoughtContent(content, filePath, basePath)` method
- `MessageType.WorkspaceChainOfThought = 'workspace_chain_of_thought'` at `message-type.ts:123`
- `MessageType.PptAgentCot = 'ppt_agent_cot'` at `message-type.ts:164`

### ask_user_question prior art
- Only exists as tool reference in ppt-agent: `modules/message-protocol/src/types/agents/ppt-agent/messages.ts:199` — comment mentions `ask_user_question tool` but no formal protocol type existed before this PR

### CoT stream event kinds align with PptAgentCot
- `turnNumber` matches `PptAgentCotPayload.turnNumber` (source: `messages.ts:292`)
- `toolName` in `CoTToolUseEvent` matches `PptAgentCotPayload.toolName` (source: `messages.ts:294`)
- `content` in `CoTThinkingEvent` matches `PptAgentCotPayload.content` (source: `messages.ts:290`)
- Missing: `PptAgentCotContentType` has `'text'` for assistant output; `CoTStreamEventKind` omits it

### Pattern: ResponseMessage for bidirectional flows
- `UserAnswerMessage` correctly uses `ResponseMessage<T>` (extends `Message<T>` with `requestId`) for client-to-agent answers
- This is the established pattern in codebase: `ResponseMessage` carries `requestId` for request correlation (source: `core.ts:39-42`)

## Gotchas

### ADO MCP tools unavailability fallback
- `mcp__azure-ado__*` tools were unavailable in this session
- Fallback: Azure CLI REST API with `az account get-access-token --resource 499b84ac-1321-427f-aa17-267ca6975798`
- Thread POST: `POST {org}/{project}/_apis/git/repositories/{repoId}/pullRequests/{prId}/threads?api-version=7.1`
- Vote PUT: `PUT .../pullRequests/{prId}/reviewers/{reviewerId}?api-version=7.1` with `{"vote":5}`
- Must use `DefaultCollection` in org URL path

### Windows bash + node template literal escaping
- Backtick template literals in node `-e` inside bash cause severe escaping issues on Windows
- Workaround: Write JSON body to temp file with node, then `curl -d @file`
- The `\`` backtick breaks bash interpretation even within node `-e` strings

### message-type.test.ts needs updating for new types
- Test file at `modules/message-protocol/tests/message-type.test.ts` has explicit per-category assertions
- The "Enum integrity" tests (no duplicates, all strings, count >= 90) will auto-pass
- But per-category describe blocks for ChainOfThoughtUpdate, AskUserQuestion, UserAnswer are missing

## Verdict
PR-4970128: **APPROVE with suggestions** (vote: 5). Clean additive protocol extension. Posted as ADO thread ID 62155400.
