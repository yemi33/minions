# Dallas Learnings — 2026-03-16 (PR-4970128 Fix Review — 8th Dispatch)

## Task
Fix review issues on PR-4970128: feat(PL-W015): add CoT streaming and ask-user-question protocol types

## Outcome
**Early bail-out — no code changes needed.** The dispatched "review finding" was implementation notes (Build: PASS | Tests: 110 passed), not actionable code review feedback.

## Findings

### 1. Engine dispatch continues to misclassify implementation notes as review issues
The review finding content was: "Added 3 new MessageType enum entries... Build: PASS | Tests: 110 passed | Downstream core: PASS" — purely informational. PR-4970128 has been dispatched for "fix review issues" at least 8 times now despite having APPROVE votes and all feedback addressed. (source: PR-4970128 thread history, 30+ threads)

### 2. Early bail-out pattern remains effective
Checking PR status + thread history via ADO REST API took ~15 seconds. A full worktree setup + yarn install + build + test + lint cycle would have taken 5-10 minutes. This is the 8th validated instance of cost savings. (source: ADO REST API GET /pullRequests/4970128/threads)

### 3. ADO temp file pattern for curl POST
On Windows bash, inline JSON with special characters in curl `-d` fails. Must write JSON to `$TEMP/file.json` then use `-d @"$TEMP/file.json"`. (source: curl POST to dev.azure.com/office/ISS/_apis/git/repositories/.../threads)

## Patterns Confirmed
- ADO PR thread status: 4 = closed, 1 = active
- Yemi Shin vote 10 = APPROVE
- dev.azure.com hostname required (not office.visualstudio.com)
- `az account get-access-token --resource 499b84ac-1321-427f-aa17-267ca6975798` for ADO token
