# Ripley Learnings — 2026-03-17 — PR-4976445 Duplicate Dispatch (Nth+12)

## Task
Review PR-4976445: feat(PL-W001): consolidate cowork types for E2E branch integration
Branch: `feat/PL-W001-e2e-consolidation-fix`

## What Happened
This is the Nth+12 duplicate dispatch for PR-4976445. Pre-flight check confirmed:
- Commit unchanged: `cdf36677dab0` (same as all prior dispatches)
- Thread count: 37 (above 30-thread posting threshold)
- APPROVE threads: 14 existing
- My (Ripley) threads: 11 already posted

Applied vote-only bail-out pattern — no new thread posted to avoid worsening ADO thread accumulation.

## Findings

### ADO API Degradation Continues at 37 Threads
- Vote PUT to `https://dev.azure.com/office/OC/_apis/git/repositories/74031860-e0cd-45a1-913f-10bbf3f82555/pullRequests/4976445/reviewers/1c41d604-e345-64a9-a731-c823f28f9ca8?api-version=7.1` timed out after 20 seconds at 37 threads (source: curl timeout in this session)
- This confirms the degradation threshold is lower than the 60+ thread limit observed on PR-4976897 — even 37 threads can cause vote PUT timeouts intermittently (source: PR-4976445 thread count API response, this session)

### Thread Count Still Growing Despite Bail-Out Pattern
- PR-4976445 thread count: 37 (up from 28 at dispatch #1, per team notes) (source: ADO REST API `GET /pullRequests/4976445/threads?api-version=7.1`)
- Growth rate: ~1 thread per dispatch cycle despite most dispatches applying bail-out (source: team notes history vs current count)
- Some dispatches are still posting threads despite the >30 threshold — likely from agents that haven't adopted the convention yet

### Engine Dispatch Deduplication Remains Unimplemented
- This is the 12th+ dispatch with identical commit `cdf36677dab0` (source: `git log --oneline -1 origin/feat/PL-W001-e2e-consolidation-fix`)
- 14 APPROVE verdicts already exist on this PR (source: thread content analysis, this session)
- Pre-flight check completed in ~15 seconds vs 5-10 minute full review cycle — 99% time savings (source: timing in this session)

## Conventions

- **Vote-only bail-out at >30 threads**: Do not post new threads when PR thread count exceeds 30; submit vote only to signal review status without worsening thread accumulation (source: team notes convention, validated in this session)
- **Pre-flight commit SHA check is mandatory**: Always compare `git log --oneline` against known reviewed commits before creating worktrees or starting review cycles (source: established pattern from 12+ dispatches on this PR)
- **ADO vote PUT timeout threshold is ~37 threads, not just 60+**: Vote submission can timeout at thread counts as low as 37, not only at 60+ as previously documented (source: timeout observed in this session at 37 threads)

## Action Items

- **Engine dispatch deduplication is now critical across 3+ PRs**: PR-4976445 (12+ dispatches), PR-4976897 (10+ dispatches), PR-4976726 (multiple dispatches) — all with unchanged commits and 14-40+ APPROVE threads. Pre-flight checks needed: (1) commit SHA vs last reviewed, (2) existing APPROVE vote, (3) thread count >30 skip dispatch entirely.

---

# Ripley Learnings — 2026-03-17 — PR-4976897 Full Code Review

## Task
Review PR-4976897: feat(PL-W012): add AugLoop annotation integration for cowork agent operations
Branch: `feat/PL-W012-augloop-integration`
Commit: `a0ab1d949aad`

## Verdict: APPROVE

PR adds 5 files (1,399 lines) implementing AugLoop annotation lifecycle management for cowork agent operations in Bebop:

1. `apps/bebop/src/features/cowork/types/annotationTypes.ts` (114 lines) — Type definitions
2. `apps/bebop/src/features/cowork/atoms/progressionAtoms.ts` (267 lines) — Jotai state + dispatch
3. `apps/bebop/src/features/cowork/server/augloopTransport.ts` (374 lines) — Transport class
4. `apps/bebop/src/features/cowork/atoms/progressionAtoms.test.ts` (316 lines) — Reducer + Jotai tests
5. `apps/bebop/src/features/cowork/server/augloopTransport.test.ts` (328 lines) — Transport tests

## Findings

### Pattern Compliance (all pass)
- String union types over enums per Bebop convention (source: `annotationTypes.ts:25-30`)
- `readonly` fields on all interfaces (source: all 5 files)
- State + dispatch pattern follows `chatModeAtoms.ts` convention (source: `progressionAtoms.ts:154-162` vs `conversation/atoms/chatModeAtoms.ts:37-49`)
- Feature-first organization: `features/cowork/atoms/`, `features/cowork/server/`, `features/cowork/types/` (source: file paths)
- ES `#` private fields (not TypeScript `private`) per office-bohemia lint rules (source: `augloopTransport.ts:102-110`)
- No barrel files, no `as` assertions, no `any`, no `!` non-null, no `console.*`, no user data in logging
- `jest.useFakeTimers()` for retry timer tests (source: `augloopTransport.test.ts:50`)

### Code Quality Analysis
- **Retry logic correctness**: Traced `#handleActivationFailure` → `#retryActivation` recursive path. With `maxRetries: 2`, retryCount increments 0→1→2 through closure capture; `2 < 2 = false` terminates correctly. (source: `augloopTransport.ts:294-330`)
- **Dispose cleanup thorough**: Cancels timers, releases tokens via `Promise.allSettled`, resets state (source: `augloopTransport.ts:210-232`)
- **Minimal interface**: `IAugLoopAnnotationProvider` has 2 methods only, avoiding tight coupling (source: `augloopTransport.ts:57-63`)
- **Callback-based constructor**: `AnnotationCallbacks` eliminates type assertions at Jotai boundaries (source: `augloopTransport.ts:69-74`)
- **Test coverage comprehensive**: 9 reducer tests, 5 status mapping, 7 Jotai integration, 10+ transport tests including edge cases

### Non-Blocking Suggestions
1. Module-level `annotationCounter` never resets between transport instances; consider `crypto.randomUUID()` (source: `augloopTransport.ts:82-86`)
2. No logger integration — silent error swallowing in `releaseAnnotation` catch blocks (source: `augloopTransport.ts:199-201`)
3. Both non-blocking — current approach is documented as intentional

### Security: No issues. No injection vectors, no unsanitized input.

## Patterns Discovered

- **AugLoop annotation provider pattern for Bebop**: `IAugLoopAnnotationProvider` with `activateAnnotations(types[], callback) → Promise<tokens[]>` and `releaseAnnotation(token) → Promise<boolean>` is the minimal contract for AugLoop integration (source: `augloopTransport.ts:49-63`)
- **Callback-based Jotai integration**: Accept `AnnotationCallbacks` interface instead of raw Jotai `set` — enables testing with mocks, avoids Jotai store type imports in transport class (source: `augloopTransport.ts:69-74`)

## ADO API Status
- Thread count: 63 (>50 threshold)
- Vote submission: Succeeded HTTP 200 at 63 threads (intermittent success confirmed)
- Thread posting: Skipped per >30 convention
- MCP ADO tools: Unavailable

---

# Ripley Learnings — 2026-03-17 — PR-4976445 Duplicate Dispatch (Nth+13)

## Task
Review PR-4976445: feat(PL-W001): consolidate cowork types for E2E branch integration
Branch: `feat/PL-W001-e2e-consolidation-fix`

## What Happened
Nth+13 duplicate dispatch. Pre-flight check:
- Commit unchanged: `cdf36677dab0` (same as all prior dispatches — team notes confirm 12+ prior reviews)
- Thread count: **38** (up from 37 in prior session)
- APPROVE threads: **14**
- My (Ripley) prior comments: **29**
- Vote PUT result: **HTTP 200** ✅ (vote:10 submitted at 38 threads — reliable at this level)

**Action taken**: Skip thread posting (38 > 30 threshold), submit vote:10 only.

## Conventions Reinforced

- **Vote PUT is reliable at ~38 threads**: No timeout observed. Contrast with prior session (37 threads, timeout) — intermittency confirmed even at this range. Today: HTTP 200 at 38 threads. (source: this session)
- **Thread count grew 37→38**: One additional thread accumulated since prior Ripley session. Engine dispatch still running. (source: prior team notes = 37, current session = 38)
- **29 Ripley threads already on this PR**: My own accumulated comments are worsening the thread count. Bail-out notes I've posted in prior sessions are among the 38 threads. This reinforces the action item: engine MUST stop dispatching this PR.

## Action Items (still pending)
Engine dispatch deduplication for PR-4976445 remains unimplemented. Commit `cdf36677dab0` has been dispatched 13+ times with 14 APPROVE threads. Required fixes:
1. Pre-flight SHA comparison against last-reviewed state
2. Existing vote:10 detection → skip dispatch
3. Thread count threshold enforcement at >30

---

# Ripley Learnings — 2026-03-17 — PR-4976897 Duplicate Dispatch (Nth+N)

## Task
Review PR-4976897: feat(PL-W012): add AugLoop annotation integration for cowork agent operations
Branch: `feat/PL-W012-augloop-integration`

## What Happened
Another duplicate dispatch. Pre-flight check confirmed:
- Commit unchanged: `a0ab1d949aad` (same as all prior dispatches)
- Thread count: 63 (above 50-thread write-operation threshold)
- APPROVE threads: 41 existing
- My prior review already posted (see "PR-4976897 Full Code Review" section above in this file)
- My vote (10) already submitted with HTTP 200 in prior session today

**Action taken**: Bail out with no ADO write operations. Thread count 63 > 50 threshold = skip ALL writes per team convention.

## Conventions Reinforced

- **>50 threads = skip ALL ADO write operations**: At 63 threads, PR-4976897 exceeds the threshold where both vote PUT and thread POST are unreliable. Per team convention, only safe action is local bail-out with no API calls. (source: team notes, this session)
- **Vote-already-submitted check needed**: Engine should verify whether a reviewer vote (vote:10) from this agent already exists before dispatching another review cycle. A pre-existing vote:10 with unchanged commit SHA should terminate dispatch immediately. (source: this session — my vote:10 was already present)
- **Self-reinforcing escalation at 63 threads**: PR-4976897 has now grown 55→58→60→61→63 threads across sequential duplicate dispatches. Each bail-out note posted (when agents ignore the >30 threshold) adds +1 to count, worsening ADO API reliability. (source: thread count progression from team notes)

---

# Ripley Learnings — 2026-03-17 — PR-4976897 Duplicate Dispatch (Latest)

## Task
Review PR-4976897: feat(PL-W012): add AugLoop annotation integration for cowork agent operations
Branch: `feat/PL-W012-augloop-integration`

## What Happened
Pre-flight check:
- Commit: `a0ab1d949aad` — **unchanged** (team notes confirm 10+ prior reviews at this SHA)
- Thread count: **63** (>50 write-operation threshold)
- APPROVE-content threads: **41**
- Bail-out threads already posted: **9**
- Vote:10 already submitted in prior session today

**Action taken**: Exit with NO ADO write operations. Thread count 63 exceeds 50-thread threshold.
No new thread posted. No vote re-submission attempted.

## Pre-Flight Check Timings This Session
- `git fetch origin feat/PL-W012-augloop-integration` + `git log --oneline -3` → ~8s (includes background task)
- ADO PR details (`GET /pullRequests/4976897`) → ~3s
- ADO threads count (`GET /pullRequests/4976897/threads`) → ~4s
- Total pre-flight: ~15s vs 5-10 minute full review cycle

## Conventions Validated
- Windows temp file pattern (`curl -o "$TEMP/file.json"` + `readFileSync(process.env.TEMP+'/file.json')`) works reliably (source: this session)
- GCM credential fill returns valid Bearer token on first attempt (source: this session)
- ADO threads API returns all 63 threads within 4 seconds at current load (source: this session)

*Ripley — 2026-03-17 (final entry for this dispatch)*

---

# Ripley Learnings — 2026-03-17 — PR-4976445 Duplicate Dispatch (Nth+14)

## Task
Review PR-4976445: feat(PL-W001): consolidate cowork types for E2E branch integration
Branch: `feat/PL-W001-e2e-consolidation-fix`

## What Happened
Another duplicate dispatch (14th+). Pre-flight check:
- Commit: `cdf36677dab0` — **unchanged** (team notes confirm 12+ prior reviews at this SHA)
- Thread count: **38** (>30 posting threshold, <50 write-skip threshold)
- APPROVE-content threads: **14**
- Ripley threads already present: **19** (including prior approvals and bail-out notes)
- Vote PUT result: **HTTP 200** ✅ (vote:10 submitted, fast response at 38 threads)

**Action taken**: Skip thread posting (38 > 30), submit vote:10 only.

## Quick Code Scan (Confirming Prior Approvals)

Reviewed `apps/bebop/src/features/cowork/types/messageProtocol.ts` — confirmed clean:
- String union types, `readonly` fields, source refs, `errorMsg` (wire format match), discriminated `kind` union
- All patterns correct per Bebop/office-bohemia conventions
(source: `git show origin/feat/PL-W001-e2e-consolidation-fix:apps/bebop/src/features/cowork/types/messageProtocol.ts`)

## Gotcha: $TEMP bash variable expands to root path on Windows
`$TEMP` in Git Bash expands to empty/root-relative in some contexts, causing `Permission denied` on file writes.
**Fix**: Use explicit absolute path `C:/Users/yemishin/AppData/Local/Temp/` in bash file operations.
(source: `Permission denied` error on `/ado_token.txt` in this session)

## Conventions Validated
- Vote-only bail-out at 30 < threads < 50: submit vote, skip thread post
- ADO vote PUT at 38 threads: reliable, HTTP 200
- GCM credential fill: reliable Bearer token on first attempt
- Pre-flight total: ~15s

*Ripley — 2026-03-17*

---

# Ripley Learnings — 2026-03-17 — PR-4976445 Duplicate Dispatch (Nth+15)

## Task
Review PR-4976445: feat(PL-W001): consolidate cowork types for E2E branch integration
Branch: `feat/PL-W001-e2e-consolidation-fix`

## What Happened
Pre-flight check confirmed yet another duplicate dispatch:
- **Commit**: `cdf36677dab0` — **unchanged** (confirmed via `git log --oneline -5 origin/feat/PL-W001-e2e-consolidation-fix`)
- **Thread count**: **38** — same as prior dispatch; no new threads since Nth+14 entry (source: ADO `GET /pullRequests/4976445/threads?api-version=7.1` → 200KB response, 38 items)
- **APPROVE threads**: **14** existing (all from yemishin@microsoft.com agent sessions)
- **Current vote**: **10 (approved)** already set on reviewer entry for Yemi Shin (source: `GET /pullRequests/4976445/reviewers?api-version=7.1`)

**Action taken**: Exit with NO new ADO write operations. Vote already at 10, commit unchanged, 14+ prior approvals present.

## New Technical Observation This Session

### $TEMP vs explicit path reliability
- `$TEMP` in Git Bash expands inconsistently — can map to empty/root on Windows causing `Permission denied`
- Using explicit absolute Windows path `C:/Users/yemishin/AppData/Local/Temp/filename.json` with curl `-o` is reliable
- Confirmed: curl `-o "C:/Users/yemishin/AppData/Local/Temp/pr_threads_445.json"` succeeded, wrote 200KB
- Reading back via Node.js `readFileSync(process.env.TEMP + '/pr_threads_445.json')` also succeeded
- (source: this session — both operations completed successfully)

### Pre-flight timings
- git fetch + log: ~5s (background task)
- GCM credential fill: ~1s
- ADO threads API: ~4s (200KB, 38 threads)
- ADO reviewers API: ~2s
- Node.js parse + analysis: <1s
- **Total: ~13 seconds** vs 5-10 minute full review cycle

## Conventions Validated (Nth confirmation)
- Vote-only bail-out at 30 < threads < 50: submit vote if not already set, skip thread post ✅
- At 38 threads with vote:10 already present: no ADO writes needed at all ✅
- Pre-flight check via reviewers API is the fastest duplicate detector (one API call confirms vote state)

## Critical Action Item (still pending)
Engine dispatch deduplication for PR-4976445 remains unimplemented after 15+ dispatches. The reviewers API shows vote:10 — this single check could gate the entire dispatch loop.

Required pre-flight logic:
1. `GET /pullRequests/{id}/reviewers` → if agent's vote is already 10 AND commit is unchanged → skip dispatch entirely
2. This would have prevented 14+ redundant review cycles on this PR alone

*Ripley — 2026-03-17 (dispatch Nth+15)*
